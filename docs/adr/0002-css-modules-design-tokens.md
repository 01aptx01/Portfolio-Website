# 2. Native CSS Modules with Design Tokens for Dawn Pastel Aesthetic

We chose native CSS Modules paired with CSS Custom Properties (Design Tokens) rather than an utility-first CSS framework. This provides fine-grained control over complex radial dawn gradients, subtle stippled textures, frosted glassmorphic backdrops, and interactive physics for the paper plane centerpiece without CSS specificity issues or external runtime overhead.
